#!/bin/bash
echo "Content-type: text/plain"
echo
curl -s -k -X POST -u {{ apilogin  }}:{{ apipassword }} -H 'Content-Type: application/json' --data '{ "extra_vars": "{\"useremailnotifications\": \"{{ useremailnotifications }}\", \"testname\": \"{{ testname }}\" }" }' '{{ apilink }}' > /dev/null 2>&1

echo "Your test environment has been started for provision"

echo '<font color="green"><h1>Task 4 - Web server</h1></font>'

#mv $0 $0.old
rm -rf $0
