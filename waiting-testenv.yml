---
  - name:
    hosts: localhost
    connection: local
    tasks:

      - name: Set of tasks to execute when testname is defined
        block:

        - name: sleep for "{{ examtime }}" seconds and continue with play
          wait_for: timeout="{{ examtime }}"
          delegate_to: localhost

#        - name: Save exam tasks details for future using
#          set_stats:
#            data:
#              exam_tasks: "{{ exam_tasks }}"

        # Endblock 
        when: testname is defined

