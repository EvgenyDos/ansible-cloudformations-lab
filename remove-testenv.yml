---
- name: cloudformation stack down
  cloudformation:
    stack_name: "{{ stack_name }}"
    state: "absent"
    region: "{{ region_name }}"
    disable_rollback: true

- debug:
    msg: vmhostip is "{{ vmhostip }}" 

