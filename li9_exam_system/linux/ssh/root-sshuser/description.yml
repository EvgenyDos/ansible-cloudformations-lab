title:  Passwordless ssh access
description : |
  Configure your system to allow passwordless access:

  * local root user should be able to do `ssh sshuser@localhost` without a password prompt.

  **Note!** Use the following command for checking `ssh sshuser@localhost id`. You can use any password for sshuser.

