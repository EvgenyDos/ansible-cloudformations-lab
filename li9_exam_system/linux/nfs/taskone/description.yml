title:  NFS server
description : |
  Configure your system as NFS server as follows:
  * NFS service exports `/nfs_export` for anyone.
  * `/nfs_export` is exported in Read-Write mode.
  * Any NFS user should have Read-Write access to the share.

