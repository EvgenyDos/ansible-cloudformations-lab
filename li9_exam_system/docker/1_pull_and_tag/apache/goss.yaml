service:
  docker:
    enabled: true
    running: true
    
command:
  docker images:
    exit-status: 0
    stdout:
    - "httpd"

command:
  docker images webserver --filter=reference="*:latest":
    exit-status: 0
    stdout:
    - "webserver"
